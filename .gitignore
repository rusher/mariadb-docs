.DS_Store

# auto-backup files
*~

# RBCommons
.reviewboardrc

# vscode
.vscode

# CLion project files
.idea

# AI local files
# CLAUDE.md is now committed and shared (see CLAUDE.md / AGENTS.md).
# Personal, machine-specific Claude Code overrides stay local:
.claude/settings.local.json
# doc-from-ticket source-repo config + fact-check reports_dir (paths differ per user/machine;
# fact-check reports themselves live OUTSIDE the repo and are never committed):
.claude/doc-sources.local.json
# bulk-campaign resumable progress state (per-run, machine-local):
.claude/bulk-campaign.local.json
# per-task research paper trail (source-verification notes, one file per Jira ID):
research/

# PDF generation (pdf/, DOCS-5710)
# Mermaid bundle is ~3.5 MB and fetched by pdf/fetch-deps.sh, not committed:
/pdf/vendor/
# Default --out-dir for the generated PDFs and their intermediate HTML.
# Anchored to the repo root so it cannot shadow a documentation page path:
/dist/
/pdf/__pycache__/
